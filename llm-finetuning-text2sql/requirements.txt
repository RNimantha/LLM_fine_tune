# Core fine-tuning stack — versions known-good for the current TRL API
# (processing_class, SFTConfig.max_length). Colab already ships torch; we don't pin it
# so we don't fight Colab's CUDA build.

transformers>=4.46.0
trl>=0.13.0
peft>=0.13.0
bitsandbytes>=0.44.0
accelerate>=1.1.0
datasets>=3.0.0

# Evaluation
sqlglot>=25.0.0

# Config + utilities
pydantic>=2.0.0
pyyaml>=6.0
huggingface_hub>=0.26.0

# Experiment tracking (TensorBoard is enough; W&B optional)
tensorboard>=2.17.0
